# 微信小程序云开发部署指南

## 1. 环境准备

### 1.1 开发工具
- 安装 [微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)
- 安装 Node.js 16+ 版本

### 1.2 微信小程序账号
- 注册 [微信小程序账号](https://mp.weixin.qq.com/)
- 开通云开发环境（[开通指南](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/getting-started.html)）

## 2. 项目配置

### 2.1 修改小程序 AppID

1. 打开 `src/manifest.json` 文件
2. 在 `mp-weixin` 配置中填写你的小程序 AppID：

```json
"mp-weixin" : {
  "appid" : "你的小程序AppID",
  "setting" : { "urlCheck" : false },
  "usingComponents" : true,
  "cloudfunctionRoot": "cloudfunctions/",
  "cloud": true
}
```

3. 同样在 `project.config.json` 中填写你的小程序 AppID：

```json
"appid": "你的小程序AppID"
```

### 2.2 配置云开发环境

1. 登录微信开发者工具，打开项目
2. 点击左上角的「云开发」按钮，创建或选择一个云开发环境
3. 记录云开发环境 ID
4. 打开 `src/main.js` 文件，修改云开发环境 ID：

```javascript
// #ifdef MP-WEIXIN
wx.cloud.init({
  env: '你的云开发环境ID', // 替换为你的云开发环境ID
  traceUser: true
});
// #endif
```

## 3. 云函数部署

### 3.1 安装依赖

1. 在 `cloudfunctions` 目录下的每个云函数文件夹中，执行：

```bash
npm install
```

### 3.2 部署云函数

1. 在微信开发者工具中，展开「cloudfunctions」目录
2. 右键点击每个云函数，选择「上传并部署：云端安装依赖」
3. 确保所有云函数都成功部署

## 4. 云数据库初始化

### 4.1 创建集合

在微信开发者工具的云开发控制台中，创建以下集合：

1. `words` - 单词表
2. `user_word_progress` - 用户单词学习进度表
3. `users` - 用户信息表

### 4.2 导入初始数据

1. 打开 `database.sql` 文件，查看数据库结构
2. 根据需要，将初始单词数据导入到 `words` 集合中

## 5. 项目构建与运行

### 5.1 构建项目

在项目根目录下执行：

```bash
npm run build:mp-weixin
```

### 5.2 运行项目

1. 打开微信开发者工具
2. 导入 `dist/build/mp-weixin` 目录
3. 点击「编译」按钮，运行小程序
4. 点击「预览」按钮，在手机上查看效果

## 6. 功能验证

### 6.1 用户登录

1. 在小程序中点击「我的」页面
2. 确保能够获取用户信息

### 6.2 单词学习

1. 在小程序中点击「学习」页面
2. 确保能够加载单词列表
3. 尝试更新单词学习进度，确保能够同步到云数据库

### 6.3 数据同步

1. 检查云数据库中的数据是否与小程序中的数据一致
2. 验证云函数调用是否正常

## 7. 常见问题与解决方案

### 7.1 云函数调用失败

- 检查云函数是否已正确部署
- 检查云开发环境 ID 是否配置正确
- 检查云函数的权限设置

### 7.2 数据库操作失败

- 检查集合是否已创建
- 检查数据库权限设置
- 确保云函数具有正确的数据库操作权限

### 7.3 构建失败

- 确保 Node.js 版本正确（16+）
- 尝试删除 `node_modules` 目录并重新安装依赖
- 检查项目配置文件是否有语法错误

## 8. 后续开发

### 8.1 添加新云函数

1. 在 `cloudfunctions` 目录下创建新的云函数文件夹
2. 创建 `index.js` 和 `package.json` 文件
3. 在 `package.json` 中添加 `wx-server-sdk` 依赖
4. 编写云函数代码
5. 上传并部署云函数

### 8.2 扩展功能

- 可以添加更多云函数来扩展功能
- 可以使用云存储来存储图片、音频等资源
- 可以使用云调用功能来调用微信开放接口

## 9. 联系与支持

如有任何问题，请参考微信小程序官方文档：
- [微信小程序开发文档](https://developers.weixin.qq.com/miniprogram/dev/framework/)
- [微信小程序云开发文档](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/getting-started.html)

---

部署完成后，你的日语单词学习小程序将运行在微信小程序云开发环境中，实现了云端数据存储和云函数调用功能。