# 产品需求文档 (PRD)

## 1. 引言

### 1.1. 目的
本文档旨在阐述一款日语词汇学习应用的功能需求。其内容通过分析现有代码库生成，旨在为产品经理和开发人员提供一份清晰的参考，详细说明应用的功能、用户流程及交互逻辑。

### 1.2. 目标读者
- **产品经理**：了解当前产品的功能特性和用户体验。
- **开发人员**：清晰了解已实现的产品逻辑，并为未来的开发提供指引。

## 2. 用户流程

### 2.1. 主导航
应用使用标签栏（Tab Bar）作为主导航，用户可切换四个核心板块：
- **首页**：作为应用的仪表盘，是学习活动的入口。
- **学习**：学习新词和复习旧词的核心界面。
- **词库**：一个可搜索、可筛选的个人词汇库（与‘我的’设置的词库关联）。
- **我的**：包含用户数据统计、应用设置和个人信息。

### 2.2. 核心学习闭环
1.  **开始**：用户从 **首页** 开始。
2.  **操作**：用户点击“开始学习”或“复习模式”。
3.  **跳转**：点击“开始学习”会跳转至 **学习** 页面。点击“复习模式”会跳转到 **学习** 页面的“复习”模式。
4.  **互动**：用户学习一个单词，并将其标记为“下一个”或“不认识”。
5.  **循环**：应用展示下一个单词。
6.  **管理**：用户可随时访问 **词库** 查看词汇、检查进度并按状态筛选。
7.  **追踪**：**我的** 页面汇总了用户的学习进度和统计数据。

---

## 3. 功能需求

### 3.1. 首页 (`pages/index/index`)
首页是应用的仪表盘，汇总了用户的学习状态，并提供了核心学习功能的快捷入口。

| 功能 | 描述 | 用户交互 |
| --- | --- | --- |
| **用户问候** | 根据当前时间显示问候语（如“晚上好”），并展示用户的头像和昵称（“Alex”）。 | 无交互，为纯展示组件。 |
| **通知中心** | 页面右上角的铃铛图标。 | 点击后应打开通知列表。当前点击会提示“功能待开发”。 |
| **学习进度** | 一个卡片，展示今日学习进度（如 15/20 新词）和连续打卡天数（如“已连续打卡 5 天”）。 | 进度条直观展示了完成度。此为纯展示组件。 |
| **每日一词** | 一个醒目的卡片，包含“每日一词”的日语、假名、释义和配图（待开发）。 | - **播放读音**：音量图标应播放单词读音。|
| **开始学习** | 一个主要的行动号召（CTA）卡片，引导用户开始学习新词，并显示待学习的单词数量。 | 点击后，跳转至 `pages/learn/learn` 页面。 |
| **复习模式** | 一个次要的行动号召卡片，引导用户复习单词，并显示需要复习的单词数量。 | 点击后，代码会尝试跳转到 `/pages/review/review` 页面。预期的行为是跳转到 **学习页面** 并选中“复习”模式。 |

### 3.2. 学习页面 (`pages/learn/learn`)
这是用户与单词卡片进行交互的核心学习界面。

| 功能 | 描述 | 用户交互 |
| --- | --- | --- |
| **模式切换** | 顶部右侧有一个开始复习按钮，可以切换到复习模式。 |
| **进度条** | 顶部的细进度条显示当前学习会话的进度（例如 15/50）。 | 随着用户学习单词的进度自动更新。 |
| **单词卡片** | 一个展示当前单词的详细卡片，包括：关联图片（暂不开发）、日语写法（`木漏れ日`）、假名、词性、释义和一个例句。 | - **播放读音**：假名旁边的音量按钮应播放读音。<br>- **收藏**：书签图标允许用户收藏或标记该单词。 |
| **操作按钮** | 屏幕底部的两个主要按钮。 | - **“不认识”**：如果用户不认识该词，则点击此按钮。应用应展示下一个单词，并可能将此词加入复习队列。<br>- **“下一个”**：用户点击此按钮表示已掌握该词。应用应展示下一个单词。 |

### 3.2.1. 复习页面 (`pages/review/review`)
此页面是复习页面。

| 功能 | 描述 | 用户交互 |
| --- | --- | --- |
| **模式切换** | 顶部的左侧有一个X图标可以选择退出复习模式。 | 
| **进度条** | 顶部的细进度条显示当前复习会话的进度（例如 15/50）。 | 随着用户学习单词的进度自动更新。 |
| **单词卡片** | 一个展示当前单词的详细卡片，包括：日语写法（`木漏れ日`）、假名和4个选项，点击选项会提示正确还是错误，并自动进入一下个单词复习。 | - **播放读音**：假名旁边的音量按钮应播放读音。<br>- **收藏**：书签图标允许用户收藏或标记该单词。 |
| **操作按钮** | 屏幕底部的一行文字，“不认识这个词”。 点击它会自动选中正确的那个选项，并自动进入下个单词复习 |

### 3.3. 词库页面 (`pages/word-bank/word-bank`)
该页面允许用户浏览、搜索和管理他们的全部词汇，当然这个词库跟'我的'页面里的词库设置有关，当用户选择初级日语，那当前词库就会是初级日语的词汇。

| 功能 | 描述 | 用户交互 |
| --- | --- | --- |
| **搜索框** | 一个文本输入框，允许用户按单词进行搜索。 | 用户输入时，实时筛选下方的单词列表。 |
| **筛选标签** | 一组横向滚动的筛选按钮，包括：“全部”、“五十音”、“易错词”、“已收藏”。 | 用户可以点击标签来对单词列表进行筛选或排序。默认选中“全部”。 |
| **单词列表** | 一个可滚动的列表，展示了用户词库中的所有单词。 | 用户可以滚动列表。点击某个单词应跳转到该词的详情页。 |
| **单词条目** | 列表中的每一项都显示了单词、假名、核心释义、状态标签（如“已掌握”、“学习中”）和一个表示掌握程度的进度条。 |

### 3.4. 我的页面 (`pages/profile/profile`)
该页面显示用户信息、学习统计数据，并提供设置入口。

| 功能 | 描述 | 用户交互 |
| --- | --- | --- |
| **个人信息** | 显示用户的头像、昵称和个性签名。 | 头像上的“编辑”按钮意味着用户可以更改他们的头像和其他个人信息。 |
| **学习统计** | 两个卡片显示核心指标：“学习总时长”和“已学单词”。 | 这些是根据用户活动自动更新的纯展示组件。 |
| **我的词库** | 包含了我选中的词库以及学习目标的完成情况（例如，已完成 30 / 2000 个单词）。 | 点击此卡片应跳转到一个页面，用户可以在这个页面里选择要学习的词库。 |
| **设置菜单** | 一个选项列表，包括：“学习提醒”、“帮助反馈”。 | 列表中的每一项都是一个按钮，应跳转到其对应的功能页面。学习提醒，可以设置周一到周日和时间。帮助反馈，用户可以提交需要反馈的内容 |
| **退出登录** | 页面底部的一个按钮。 | 点击后，将用户登出应用，并应返回到登录界面。 |

## 4. 核心代码逻辑
1.用户的单词学习方案需要基本满足艾宾浩斯遗忘曲线，所以单词分为新词、复习、学习中、已掌握以及已收藏几个标签，用户在学词的过程中不断深入增强记忆达到真正的掌握状态。
2.当用户在学习页面学完一个单词时，单词会进入复习状态，用户在之后的第1、2、4、7天（答对后间隔1、1、2、3）会出现在复习列表，若用户在复习单词的时候答错，那这个单词会在次日再次出现在复习单词列表，如果答对，那就顺延进入第2或者应该进入的阶段。所以学习单词的进度分别是20%（刚学过），40%（过了复习第1阶段），60%（过了复习第2阶段），80%（过了复习第3阶段），100%（过了复习第4阶段）
